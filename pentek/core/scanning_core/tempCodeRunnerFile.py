 # if open_ports:
    #     print(f"[+] Found {len(open_ports)} open/filtered ports: {open_ports}")
    #     for port, state in open_ports:
    #         run_detailed_scan(target, port)
    # else:
    #     print("[!] No open or filtered ports found.")